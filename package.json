{
  "name": "pilot",
  "displayName": "pilot",
  "publisher": "nartaliti",
  "description": "A totally free, self hosted ai assistant",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.102.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "pilot.helloWorld",
        "title": "Hello World"
      },
      {
        "command": "pilot.triggerInlineCompletion",
        "title": "Pilot: Rerun Inline Completion"
      }
    ],
    "keybindings": [
      {
        "command": "pilot.triggerInlineCompletion",
        "key": "ctrl+shift+space",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "title": "Pilot AI",
      "properties": {
        "pilot.serverUrl": {
          "type": "string",
          "default": "http://localhost:11434",
          "description": "Server URL"
        },
        "pilot.model": {
          "type": "string",
          "default": "",
          "description": "AI model name (codellama:instruct)"
        },
        "pilot.embeddingModel": {
          "type": "string",
          "default": "",
          "description": "This model is used to embed project code to the AI (nomic-embed-text:latest)"
        },
        "pilot.systemMessage": {
          "type": "string",
          "default": "You are a strict code completion assistant used in a code editor.\n\n<BEHAVIOR>\n- You must return only the continuation of the code.\n- NEVER repeat or rewrite code that is already present.\n- Your output will be inserted directly at the current cursor.\n- If a function is partially written, continue it without repeating the header or prior lines.\n</BEHAVIOR>\n\n<FORMATTING>\n- Do not wrap your response in markdown.\n- Output only valid code — no comments, explanations, or repeated lines.\n</FORMATTING>",
          "editPresentation": "multilineText",
          "markdownDescription": "Defines the system prompt that guides the AI's behavior. This message is prepended to every conversation and should be written carefully to shape the assistant's tone, role, or limitations."
        }
      }
    },
    "views": {
      "main": [
        {
          "id": "main",
          "name": "Panel",
          "icon": "/resources/icon.svg",
          "type": "webview"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "main",
          "title": "Pilot",
          "icon": "/resources/icon.svg"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.102.0",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.25.3",
    "eslint": "^9.25.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "axios": "^1.11.0"
  }
}
